{
  "name": "rst-website",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build",
    "start": "cd app && npm run start",
    "lint": "cd app && npm run lint",
    "check:imports": "node ./scripts/check-case-sensitive-imports.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
